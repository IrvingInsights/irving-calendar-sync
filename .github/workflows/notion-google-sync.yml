name: Notion to Google Calendar Sync
on:
  # schedule:
  # - cron: '*/5 * * * *'  # Runs every 5 minutes - DISABLED until Google credentials are configured
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install notion-client==2.2.1 google-api-python-client google-auth-httplib2 google-auth-oauthlib
      - name: Run Notion to Google Calendar sync
        env:
          NOTION_API_TOKEN: ${{ secrets.NOTION_API_TOKEN }}
          NOTION_TASKS_DB_ID: ${{ secrets.NOTION_TASKS_DB_ID }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          DOMAIN_CALENDAR_MAPPING: ${{ secrets.DOMAIN_CALENDAR_MAPPING }}
          TIMEZONE: 'America/New_York'
        run: |
          python notion_google_sync.py
